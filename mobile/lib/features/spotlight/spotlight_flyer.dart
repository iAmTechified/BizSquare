import 'package:flutter/material.dart';

class SpotlightFlyerScreen extends StatelessWidget {
  const SpotlightFlyerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Spotlight')),
      body: const Center(child: Text('Spotlight Flyer Screen')),
    );
  }
}
